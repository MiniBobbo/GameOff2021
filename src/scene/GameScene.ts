import Phaser from 'phaser'

export class GameScene extends Phaser.Scene {
    p:{x:number, y:number};
    preload() {
        
    }

    create() {

    }

    update(time:number, dt:number) {

    }
}